import {Component} from "react"

class Flags extends Component
{
    constructor(props)
    {
        super(props);
        this.state = {sort: "name"};
    }
    render()
    {
        return(
            <ul>
                
            </ul>
        );
    }
}

export default Flags;